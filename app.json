{
  "name": "iati",
  "description": "iati standard website",
  "scripts": {
    "dokku": {
      "predeploy": "python manage.py migrate --noinput && python manage.py update_index && python manage.py compilemessages"
    }
  }
}

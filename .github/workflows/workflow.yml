name: CI

on:
  # Trigger the workflow on push or pull request,
  # but only for the master/develop branches
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Pull static images
        run: docker-compose pull
      - uses: satackey/action-docker-layer-caching@v0.0.11
        continue-on-error: true
      - name: Setup docker-compose
        run: |
          docker-compose build
          docker-compose up -d
          while ! echo exit | nc localhost 5000; do sleep 10; done
      - name: flake8 lint
        run: docker-compose exec -T web make flake8
      - name: pylint
        run: docker-compose exec -T web make pylint
      - name: pydocstyle lint
        run: docker-compose exec -T web make pydocstyle
      - name: pytest
        run: docker-compose exec -T web make test
